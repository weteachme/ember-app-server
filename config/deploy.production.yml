# Kamal configuration for ember-app-server (Staging - default)
service: ember-app-server-production

proxy:
  host: dashboard.weteachme.com

# Volume mount for staging
volumes:
  - /data/ember-app/production:/ember-app
